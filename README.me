# JwtProviderWS
The aim of the web service is to the authentication and generate the JWT token on successful authentication, which could later be used for the accessing the secured rest path on the web service itself.
